# pnpm >=10 bloquea por defecto instalar paquetes publicados hace menos de 24h
# (protección de supply-chain "minimumReleaseAge"). La desactivamos a nivel de
# proyecto porque bloqueaba builds limpios
minimum-release-age=0
